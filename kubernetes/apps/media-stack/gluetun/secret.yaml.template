# Gluetun VPN Secret Template
# Copy this file to secret.yaml and fill in values
# Then seal it with: kubeseal < secret.yaml > sealed-secret.yaml
#
# DO NOT commit the unsealed secret.yaml to git!

apiVersion: v1
kind: Secret
metadata:
  name: gluetun-vpn-secret
  namespace: media-stack
type: Opaque
stringData:
  # Mullvad WireGuard private key
  wireguard-private-key: "YOUR_WIREGUARD_PRIVATE_KEY_HERE"
